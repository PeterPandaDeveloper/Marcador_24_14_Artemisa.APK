name: contador24s_utm
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.11.0 <4.0.0' # Esta es la línea que te falta

dependencies:
  flutter:
    sdk: flutter
  flutter_bluetooth_serial: ^0.4.0
  permission_handler: ^11.3.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/balon.png
    - assets/images/LOGO_OUT.png